# ETB Shaka Cast Receiver v5

Stable step build: no black overlay, TV-focusable two-row controls, autohide, buffer health rail, audio/subtitle/quality menus, optional Cloudflare Access headers from `customData.headers`, and debug only with `debug: true` or `debugOnError: true`.
